@*@model Temiskaming.Models.chatSendModel*@
@{
    Layout = null;
}

@using (Ajax.BeginForm("Send", "Chat", FormMethod.Post, new AjaxOptions
{
    HttpMethod = "Post",
    OnSuccess = "$('#message').val('')"
    
}, new { @id = "fMessage" }))
{

    @Html.TextBoxFor(x => x.message)
    <input type="submit" id="send" name="send" value="Send" />
}


