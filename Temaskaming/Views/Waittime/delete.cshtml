@model Temiskaming.Models.waittime

@{
    ViewBag.Title = "delete";
}

<h2>Confirm to patient @Model.name?</h2>

@using (Html.BeginForm())
{
    <input type="submit" value="Yes"/>
    @Html.ActionLink("Cancel", "admin")
}